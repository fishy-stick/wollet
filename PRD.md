# Wollet 首版 PRD

## 1. 项目简介

Wollet 是一个纯内网使用的 Windows 远程开关机工具。名字由 WOL（Wake-on-LAN）与后缀 -let 组合而来，意为一个小巧、专注的 Wake-on-LAN 工具。

它由 Linux 服务端和 Windows 客户端组成。Linux 服务端提供 Web 管理页面；Windows 客户端负责上报设备状态，并接收关机指令。用户也可以通过 Linux 服务端发送 Wake-on-LAN 数据包来唤醒已经关机的 Windows 电脑。

外出时，用户先通过 VPN 接入家中内网，再访问 Wollet，不需要公网直连、端口映射或 NAT 穿透。

## 2. 使用环境

- Linux 服务端和 Windows 电脑位于同一个内网。
- 当前网络环境已经测试支持 Wake-on-LAN。
- Windows 电脑的 BIOS、网卡和系统已经正确配置 WoL。
- 系统需要支持管理多台 Windows 电脑。

## 3. Linux 服务端

Linux 服务端需要：

- 作为常驻服务运行。
- 提供 Web 管理界面。
- 按需生成用于绑定 Windows 客户端的一次性 Token。Token 固定有效期为 5 分钟，绑定成功后立即失效，过期后由服务端自动删除。
- 接收 Windows 客户端注册及连接。
- 保存设备名称、MAC 地址和连接状态等信息。
- 展示所有已绑定的 Windows 设备。
- 展示设备在线或离线状态。
- 向指定设备发送 Wake-on-LAN 魔术包。
- 向在线设备发送远程关机指令。
- 支持管理和移除已绑定设备。

## 4. Windows 客户端

Windows 客户端需要保持安装简单、界面简洁，并可靠地在后台运行。首版优先交付为一个可执行文件，同时支持安装界面和后台服务两种运行方式；如果实现阶段发现其他内部组织方式能明显降低复杂度，也可以在不改变用户流程和安全边界的前提下调整。

本 PRD 不限定 Windows 客户端必须使用与服务端相同的语言或复用服务端代码。

### 安装与绑定界面

双击客户端时打开极简 GUI。为简化首版实现，推荐在打开界面前由 Windows 请求管理员权限；如果采用其他安全、清晰的提权流程，也可以调整请求权限的具体时机。界面只需包含：

- Linux 服务端地址输入框。
- 绑定 Token 输入框。
- “安装并绑定”按钮。
- 简单的进行中、成功或失败状态提示。

点击“安装并绑定”后，程序完成设备绑定、保存配置、安装 Windows Service 并启动服务。只有在这些步骤均成功后才能提示安装成功；成功后 GUI 可以直接关闭，不需要常驻托盘。

### 后台服务

- 客户端必须安装为自动启动的 Windows Service。
- 服务不依赖用户登录，在登录界面或无人登录时也能运行和执行关机。
- 服务启动后自动连接 Linux 服务端。
- 首次绑定后保存连接配置，后续不需要重复输入 Token。
- 配置和设备凭据保存在系统级目录，例如 `%ProgramData%\Wollet\`，不能依赖某个用户的目录。
- 上报计算机名称和用于 WoL 的网卡 MAC 地址。
- 定期上报心跳，让服务端判断设备是否在线。
- 断线后自动尝试重连。
- 接收 Linux 服务端发送的关机指令并关闭电脑。
- 服务以具备系统关机权限的账户运行。

## 5. 主要使用流程

### 安装和绑定设备

1. 用户在 Web 页面生成一个一次性 Token。Token 仅展示本次生成结果，有效期为 5 分钟。
2. 用户双击 Windows 客户端，进入安装与绑定流程。
3. 用户根据客户端提示确认 Windows 的管理员权限请求，填写 Linux 服务端地址和 Token，并点击“安装并绑定”。
4. 客户端连接服务端，完成绑定并上报设备名称和 MAC 地址。
5. 客户端保存配置，注册并启动 Windows Service。
6. 新设备出现在 Web 管理页面中，GUI 提示安装成功后退出。

### 远程开机

1. 用户在 Web 页面选择一台离线设备。
2. 点击“唤醒”。
3. Linux 服务端根据保存的 MAC 地址发送 WoL 魔术包。
4. Windows 开机后，客户端服务在无人登录的情况下自动启动并连接服务端。
5. Web 页面中的设备状态变为在线。

### 远程关机

1. 用户在 Web 页面选择一台在线设备。
2. 点击“关机”并确认。
3. Linux 服务端向 Windows 客户端服务发送关机指令。
4. 客户端服务执行系统关机，无需有用户登录。
5. 连接断开后，页面状态变为离线。

## 6. 首版范围

首版需要实现：

- Token 绑定设备。
- Windows 极简安装与绑定 GUI。
- Windows Service 自动安装和开机自启。
- 无用户登录时保持连接和执行关机。
- 多设备管理。
- 设备信息与在线状态展示。
- Wake-on-LAN 远程开机。
- Windows 远程关机。
- 心跳和断线重连。

首版暂不实现：

- 执行任意 Windows 命令。
- 常驻托盘程序或复杂的 Windows 管理界面。
- 公网直接访问。
- NAT 穿透或互联网中继服务。
- 跨子网或跨 VLAN 的 WoL 转发。
- 文件传输、远程桌面等其他远程控制功能。

## 7. 基本安全要求

虽然系统只在内网使用，仍需避免其他内网设备随意绑定或控制电脑：

- Token 和设备凭据必须使用密码学安全的随机数生成，服务端只保存哈希值。
- 已绑定设备应使用独立身份连接服务端。
- Web 管理页面应提供基本的访问认证。
- 关机操作应有二次确认。
- 服务端不能接受未绑定客户端发送的状态信息。
- Windows 上保存的设备凭据只能由管理员和客户端服务访问。

## 8. 关键业务规则

### Token

- Token 由管理员在 Web 页面按需生成，固定有效期为 5 分钟。
- Token 只能成功绑定一台设备；并发使用时，只有第一个完成校验的绑定请求可以成功。
- 服务端只保存 Token 哈希，Token 明文只在生成时展示一次。
- 绑定成功后立即删除 Token；未使用的 Token 过期后由服务端自动删除。
- 服务端不提供 Token 列表、查询、撤销或删除接口。

### 设备绑定

- 每次成功绑定都会生成独立的设备 ID 和设备凭据，设备名称和 MAC 地址不作为身份凭据。
- Windows 本地已有有效配置时，不重复绑定或消耗新 Token。
- 设备凭据失效后，可以使用新 Token 重新绑定并覆盖本地旧配置。
- 绑定或安装失败时，GUI 必须显示明确原因，不能提示安装成功。

### MAC 地址选择

- 客户端自动选择访问 Linux 服务端时所使用网卡的 MAC 地址。
- 客户端忽略未启用、回环以及无法用于局域网通信的网卡。
- 无法确定有效 MAC 地址时终止绑定并提示用户，首版不提供手动选择网卡功能。
- 设备每次连接时重新上报设备名称和 MAC 地址，服务端保存最新信息。

### 在线状态和重连

- 设备使用有效凭据连接成功后标记为在线。
- 客户端每 15 秒发送一次心跳；连接断开或连续 45 秒未收到心跳时，服务端将设备标记为离线。
- 同一设备只保留一个有效连接，新连接建立后关闭旧连接。
- 客户端断线后自动重连，重连间隔逐步增加，最长每 30 秒尝试一次。

### 移除设备

- 移除设备时立即撤销其设备凭据并断开现有连接。
- 被移除的客户端不能继续上报状态或接收指令。
- 移除设备不会远程卸载 Windows Service。
- 如需重新使用该设备，必须通过 GUI 使用新 Token 重新绑定。

### 远程关机

- 只有在线设备可以执行关机操作，用户必须在 Web 页面二次确认。
- 关机指令只发送给当前连接，不保存、不排队、不在设备重新上线后补发。
- 客户端确认收到指令后执行系统关机。
- 页面只能提示“关机指令已送达”；设备变为离线后，才表示关机流程完成。
- 客户端只接受固定的关机指令，不接受任意命令或参数。

### Wake-on-LAN

- 只有离线设备显示或启用“唤醒”操作。
- 服务端使用设备最后一次上报的 MAC 地址，向配置的 IPv4 广播地址发送魔术包。
- 页面提示“唤醒数据包已发送”，不直接提示“开机成功”；设备重新连接并变为在线后，才视为唤醒成功。
- 首版不支持跨子网或跨 VLAN 的 WoL 转发。

### Web 管理

- 首版只支持一个管理员，不提供用户、角色和权限管理。
- 未认证用户不能查看设备、生成 Token 或执行设备控制操作。
- 在线设备显示“关机”操作，离线设备显示“唤醒”操作。
- 操作进行中时暂时禁用对应按钮，避免重复提交。
- 服务端返回的失败原因应以简短、可理解的文字展示。

### 首版兼容范围

- Windows 客户端支持 Windows 10 和 Windows 11 64 位版本。
- Linux 服务端首版支持 64 位 Linux。
- Web 页面支持当前版本的 Microsoft Edge 和 Google Chrome。
- Linux 服务端与 Windows 设备必须位于同一个可广播的 IPv4 内网。
- 不保证虚拟机、无线网卡及受网络策略限制的环境能够使用 Wake-on-LAN。

## 9. 首版验收标准

- 可以在 Linux 上启动服务并通过浏览器打开管理页面。
- 可以生成 Token，并通过 Windows GUI 绑定一台或多台电脑。
- 生成的 Token 只能成功绑定一次，超过 5 分钟后不能继续使用。
- 已使用和已过期的 Token 会被服务端自动删除，且 Web 页面不展示 Token 列表。
- Windows 客户端在安装过程中能正确请求管理员权限，点击“安装并绑定”后能正确绑定设备、保存配置并启动 Windows Service。
- Windows 重启且无人登录时，客户端服务能够自动运行并重新上线。
- 在 Windows 登录界面也可以从 Web 页面成功执行关机。
- Web 页面能够正确显示设备在线和离线状态。
- 可以从 Web 页面成功唤醒已关机的 Windows 电脑。
- 客户端断网后能够自动重连，服务端能够更新其状态。
- 未绑定的客户端不能控制设备或伪造在线状态。
